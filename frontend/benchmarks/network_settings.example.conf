# key=value
# optional keys:
# sim_delay_ms, sim_bw_MBps, network_type, network_topology, link_speed, network_note

sim_delay_ms=2.0
sim_bw_MBps=10.0
network_type=simulated
network_topology=same-host
link_speed=10MBps
network_note=local simulated delay and bandwidth cap
